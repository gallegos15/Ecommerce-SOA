<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /Usuarios-service/

    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    RewriteRule ^(.*)$ index.php?url=$1 [QSA,L,B]
</IfModule>
